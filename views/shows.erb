<!DOCTYPE HTML>
<html lang="en-US">
<head>
	<meta charset="UTF-8">
	<title>Concerts Listing</title>
</head>
<body>
	<table>
	<thead>
		<tr>
			<th>Date</th>
			<th>Event</th>
			<th>Time</th>
			<th>Price</th>
		</tr>
	</thead>
	<tbody>
		<% @concerts.each do | concert | %>
		<tr>
			<td><%=concert.at_css('.dates').text.strip %></td>
			<td><%= concert.at_css('.headliners').text.strip %></td>
			<td><%= concert.at_css('.times').text.strip %></td>
			<td><%= concert.at_css('.ticket-price').text.strip.match(/\$[\d]+/) %></td>
		</tr>
		<% end %>
	</tbody>
	</table>
</body>
</html>
